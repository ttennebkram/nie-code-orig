This is a very tiny, mostly empty configuration.

It is NOT intended as a template, it's missing too many things.

It's ONLY purpose is to express our default <include> structure for
new configuratio.
